<div class="main">

<header>
	<h1>🐬 tags</h1>
</header>

<%= render "articles/navbar" %>

<ul>
  <% @tag.sort_by(&:name).each do |t| %>
    <li>
      <%= render t %> (<span style="color: var(--main-color);"><%= pluralize(t.articles.size, "post") %></span>)
    </li>
  <% end %>
</ul>

<hr>

<% if user_signed_in? %>
	<a href="/tags/new">new tag</a>
<% else %>
<% end %>

</div>
