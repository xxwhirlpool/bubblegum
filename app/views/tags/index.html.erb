<div class="main">

<header>
	<h1>🐬 tags</h1>
</header>

<%= render "articles/navbar" %>

<ul>
  <% @tag.each do |tag| %>
    <li>
      <%= render tag %> (<span style="color: var(--main-color);"><%= tag.articles.size %><span class="access-hidden"> post(s)</span></span>)
    </li>
  <% end %>
</ul>

<hr>

<% if user_signed_in? %>
	<a href="/tags/new">new tag</a>
<% else %>
<% end %>

</div>
