<div class="main">

<header>
	<h1 style="margin-bottom:.5rem;">🐬 <span class="p-name">sweet like bubblegum</span></h1>
</header>

<%= render "navbar" %>

<div class="profile">

<% if user_signed_in? %>
    <div class="pfp"><%= render @profiles %></div>
<% else %>
    <div class="pfp"><%= render @profiles %></div>
<% end %>

<div class="test2">
<% if user_signed_in? %>
 <div> welcome, <span style="color:var(--main-color);"><%= current_user.email %></span>! </div> 
  <%= button_to "log out", destroy_user_session_path, method: :delete, style: 'margin-left: .5em !important' %> 
<% else %>
 <div> i'm kat! </div>
  <%= button_to "log in if you're me", new_user_session_path, style: 'margin-left: .5em !important' %> 
<% end %>
</div>

<div class="test2">
<% if user_signed_in? %>
    <ul>
	    <li><%= link_to "new post", new_article_path %></li>
	    <li><%= link_to "edit profile", edit_profile_path(id: current_user) %></li>
    </ul>
<% else %>
    <ul>
  	 	<li><a href="/profiles/1">me</a></li>
 		<li><%= link_to "refresh", articles_path %></li>
    </ul>
<% end %>
</div>

</div>

<hr>

<p><%= link_to "tags", tags_path %> | <a href="https://subscribeopenly.net/subscribe/?url=https://bubblegum.girlonthemoon.xyz/articles.xml">feed</a> | <a href="https://bytes.4-walls.net/kat/bubblegum">source</a> | <%= Article.public_count %> total posts </p>

<h4>2025</h4>

<dl class="h-feed hfeed">
   <% Article.where(created_at: Date.parse("2025-01-01")..Date.parse("2025-12-31")).reverse.each do |article| %>        
     <% unless article.archived? %>
       <dt>
       		<time <%= tag.attributes datetime: article.created_at.strftime('%Y-%m-%d') %> ><%= article.created_at.strftime('%Y %b %d') %></time>
       </dt>
       <dd class="h-entry">
         <%= link_to article.title, article, class: "u-url p-name" %>                                                        
       </dd>

     <% end %>
   <% end %>                        
 </dl>

<h4>2024</h4>

<dl class="h-feed hfeed">
  <% Article.where(created_at: Date.parse("2024-01-01")..Date.parse("2024-12-31")).reverse.each do |article| %>
    <% unless article.archived? %>
      <dt>
     	 <time <%= tag.attributes datetime: article.created_at.strftime('%Y-%m-%d') %> ><%= article.created_at.strftime('%Y %b %d') %></time>
      </dt>
	  <dd class="h-entry">
         <%= link_to article.title, article, class: "u-url p-name" %>
      </dd>
    <% end %>
  <% end %>
</dl>

<%= render "articles/hcard" %>

</div>
